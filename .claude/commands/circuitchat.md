---
description: Start a new CircuitChat session — design a KiCad PCB project end-to-end via the circuitchat skill.
---

Activate the `circuitchat` skill and begin a new session. If `.kicraft/state.json` already exists in the current directory, read it and resume from there. Otherwise create the directory and start from an empty state, then ask the user what they want to build.

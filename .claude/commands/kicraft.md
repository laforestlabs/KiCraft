---
description: Start a new KiCraft session — design a KiCad PCB project end-to-end via the kicraft skill.
---

Activate the `kicraft` skill and begin a new session. If `.kicraft/state.json` already exists in the current directory, read it and resume from there. Otherwise create the directory and start from an empty state, then ask the user what they want to build.

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Manual end-to-end CircuitChat test runs (one dir per run)
/tests/manual-runs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# KiCad experiment artifacts (generated by pipeline)
.experiments/

# CircuitChat per-project session state (not portable; .kicraft/parts/ is
# intentionally not ignored so curated project-local parts ship with the repo).
.kicraft/state.json
.kicraft/session_id

# Default CircuitChat synthesis output dir
/generated/

# Logs
logs/
*.log

# Auto-generated by GUI experiment_runner on every run
kicraft/cli/program.md

# Claude Code sandbox bind-mounts (sensitive paths masked as /dev/null
# at the project root). These aren't real project files; ignoring them
# keeps `git status` clean across sessions.
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.mcp.json
/.profile
/.ripgreprc
/.zprofile
/.zshrc
/.idea
/.vscode
/.claude/agents
/.claude/settings.json
/.claude/settings.local.json
